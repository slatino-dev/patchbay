//! API-key authentication middleware.
//!
//! TODO(phase-B):
//!   - bearer-token extraction from `Authorization` header
//!   - key → identity + tier lookup (in-memory map loaded from config)
//!   - reject unknown or revoked keys with 401
//!   - pass identity to budget and limits layers

// Placeholder — no logic yet.
